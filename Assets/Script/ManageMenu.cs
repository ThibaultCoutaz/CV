using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManageMenu : MonoBehaviour {

	// Use this for initialization
	void Start () {
        HUDManager.Instance.displayMenu(true);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
